CAREER CROX FIX NOTES

This package fixes the issues requested from the Word file:

1. Import / Export / Template buttons now show only for Manager.
2. Candidate list rows are tighter and more AppSheet-like.
3. Text action buttons were replaced with icon actions in candidate rows.
4. Clicking anywhere on a candidate row opens the full profile.
5. Candidate profile now opens in a table-style detail sheet.
6. Theme changes are now visually stronger across the whole CRM.
7. Candidate import/export/template APIs are manager-only on the backend too.

Render:
- Build Command: npm install
- Start Command: npm start
