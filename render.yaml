services:
  - type: web
    name: career-crox-interns-crm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --keep-alive 5 --timeout 90 --max-requests 300 --max-requests-jitter 30
    plan: free
